# DEPRECATED — not used on Raspberry Pi OS Desktop.
# cage needs a no-desktop (Lite) setup. On Desktop we launch Chromium kiosk
# inside the running desktop session via XDG autostart instead.
# See galle-kiosk.sh + galle-kiosk.desktop and the README.
